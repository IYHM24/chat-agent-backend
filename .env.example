# Example environment variables for this project
# Copy this file to .env and fill in real values (do NOT commit .env)

# Server
PORT=3000
NODE_ENV=development

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chat_db
DB_USER=chat_user
DB_PASSWORD=your_db_password_here
DB_DIALECT=mysql

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRE=7d

# CORS - multiple origins separated by commas (include protocol)
# Example: http://localhost:3000,http://127.0.0.1:5500,http://192.168.2.9:5500
CORS_ORIGIN=http://localhost:3000

# API
API_VERSION=v1

# LLM provider selection: 'auto' | 'ollama' | 'openrouter'
LLM_PROVIDER=auto

# Ollama (local) - used when OPENROUTER_API_KEY is not set or provider forced to 'ollama'
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=phi3
OLLAMA_TIMEOUT=3600000

# OpenRouter (cloud) - only required if you want to use OpenRouter/OpenAI-compatible providers
# To enable OpenRouter set OPENROUTER_API_KEY below (keep secret, don't commit to git)
OPENROUTER_API_KEY=<Api_Key_Generada>
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free # Nombre del modelo en OpenRouter
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_TIMEOUT=60000

# Optional OpenRouter headers (optional)
OPENROUTER_SITE_URL=https://your-site.example
OPENROUTER_APP_NAME=YourAppName

# Notes:
# - After copying to .env, fill sensitive values and keep .env out of version control.
# - Use FULL origins (including http:// or https://) in CORS_ORIGIN.
